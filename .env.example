# Copy this file to .env and replace values before running
# Example local MongoDB (default used if this is not provided):
MONGO_URI=mongodb://localhost:27017/schoolDB

# Optional: port for the Express server
PORT=5000
